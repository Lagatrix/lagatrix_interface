export enum Endpoints {
  DISK = 'storage/disk',
  CPU = 'hardware/cpu',
  GPU = 'hardware/gpu',
  RAM = 'hardware/ram',
  USER = 'user',
  GROUP = 'group',
  HOST = 'host',
  CRONTAB = 'crontab',
  LOGIN = 'user/login'
}
